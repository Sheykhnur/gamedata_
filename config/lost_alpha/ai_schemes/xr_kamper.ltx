;=====================================================================
;--/ for scheme: "..." (xr_kamper.script)
;--/ Last edit: 30.06.2012
;=====================================================================

[options]
enabled = true

[scheme_params] ;--/ массив параметров для регистрации схемы
logic   = true  ;--/ используется ли схема 'штатной' логикой (имеет ли секции в *.ltx)
enable  = false ;--/ признак/флаг разрешения штатно включать схему (менеджером)
disable = false ;--/ признак/флаг разрешения штатно выключать схему (менеджером)
common  = false ;--/ признак/флаг регистрации в "addCommonProperty"
ev_ids  = 0     ;--/ id-массив эвалюаторов для "addCommonProperty"


[excluded_community]

[excluded_npcs]
