;=====================================================================
;--/ Garbage cleaner module
;=====================================================================

[options]
max_weapons                =  30 ; Maximum weapons lying around on the level.
max_items                  = 100 ; Maximum non-weapon items lying around on the level.
min_dist_to_actor          = 100 ; Minimum distance to actor from which to remove garbage

total_cleanup_min_age      = 1440 ; 24h ; Minimum minutes from the moment object was dropped to remove it (regardless of limits)

;=================================================
